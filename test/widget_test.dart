// Basic smoke test for the app.

import 'package:flutter_test/flutter_test.dart';

void main() {
  // NOTE: Full widget tests require providing QuestionService and
  // WindowManager mocks. For now, verify core model logic.

  test('Game phase transitions correctly', () {
    // Placeholder: will be replaced with proper model tests
    expect(true, isTrue);
  });
}
